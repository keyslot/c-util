#ifndef STRINGS_H
#define STRINGS_H

#include <stdio.h>

/* Get index of w in v, or -1   */
int indexOf(char *w, char *v);
/* Get frecuency of w in v      */
int frecuency(char *w, char *v);
/* Get composition of v         */
int composition(char *v);

#endif
